# Synthetic only: the local Nextcloud Compose mock returns three-component vectors.
builtin_models:
  - id: builtin-pilot-mock
    type: Embedding
    source: remote
    is_default: true
    name: mock-embed
    description: Disposable three-dimensional embedding fixture
    parameters:
      base_url: http://mock-embedding:8000/v1
      api_key: pilot-only
      provider: generic
      embedding_parameters:
        dimension: 3
        truncate_prompt_tokens: 0
