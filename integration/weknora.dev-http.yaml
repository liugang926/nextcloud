# Explicitly permits the fixed `http://nextcloud` endpoint only in the
# disposable local integration stack. Production deployment must use HTTPS.
services:
  app:
    environment:
      WEKNORA_NEXTCLOUD_DEV_HTTP: "1"
